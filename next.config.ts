import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Turbopack automatically handles native modules like 'pg' and 'pg-native'
  // No webpack configuration needed when using Turbopack
};

export default nextConfig;
